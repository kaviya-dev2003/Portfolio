{
  "build": {
    "builder": "nixpacks",
    "buildCommand": "npm install --prefix server",
    "runCommand": "npm start --prefix server"
  },
  "deploy": {
    "startCommand": "node server/server-prod.js",
    "healthcheckPath": "/api/health",
    "restartPolicyType": "ON_FAILURE"
  }
}